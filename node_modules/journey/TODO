TODO

- Implement `bindResource`.
- Ability to bind multiple functions to a route.
- `Router.prototype.push`
- Returning a value from a bound function passes it to the
  next matching route. This can function like middle-ware,
  or filters.
- Benchmark!
